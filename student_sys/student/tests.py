from django.test import TestCase

# Create your tests here.
from .models import Student

# 创建测试数据
# for i in range(6):
#     student = Student.objects.create(name=str(i), profession="程序员", email=f"{i}@{i}.com", qq=f"{i}" * 10,
#                                      phone=f"{i}" * 11)
